- Fix #268: firmware and hardware metadata updates now use a device lookup scoped to the owning config entry.
- Consolidate model-capability handling across the integration and Effect Studio.
- **Home Assistant Core 2026.9.0 or newer is now required.**

Update through HACS or the release ZIP, then restart Home Assistant fully.  No device reconfiguration is required.
Basic live control and reload checks passed on H617A and H6199.
