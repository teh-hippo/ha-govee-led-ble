{
  "config": {
    "step": {
      "bluetooth": {
        "description": "Govee {name} found via Bluetooth"
      },
      "user": {
        "data": {
          "address": "BLE Address",
          "model": "Device Model"
        }
      }
    },
    "abort": {
      "already_configured": "Device is already configured"
    }
  },
  "entity": {
    "number": {
      "video_saturation": {
        "name": "Video saturation"
      },
      "video_sound_effects_softness": {
        "name": "Video sound effects softness"
      },
      "music_sensitivity": {
        "name": "Music sensitivity"
      }
    },
    "select": {
      "video_capture_region": {
        "name": "Video color mapping"
      }
    },
    "switch": {
      "video_sound_effects": {
        "name": "Video sound effects"
      }
    }
  },
  "exceptions": {
    "unsupported_model": {
      "message": "{service} is only supported on H6199, not {model}"
    }
  },
  "services": {
    "set_video_mode": {
      "name": "Set video mode",
      "description": "Configure the H6199 video (camera sync) mode with adjustable parameters.",
      "fields": {
        "mode": {
          "name": "Mode",
          "description": "Video sub-mode: movie or game."
        },
        "saturation": {
          "name": "Saturation",
          "description": "Color saturation (0–100)."
        },
        "brightness": {
          "name": "Brightness",
          "description": "Video mode brightness level (0–100)."
        },
        "capture_region": {
          "name": "Color mapping",
          "description": "How screen colors are mapped to the strip: full applies a single dominant screen color across the strip, part maps colors by screen zones."
        },
        "full_screen": {
          "name": "Use full mapping",
          "description": "true = full mapping (single dominant color across strip), false = part mapping (zoned screen-to-strip mapping)."
        },
        "sound_effects": {
          "name": "Sound effects",
          "description": "Enable audio-reactive overlay on top of video mode."
        },
        "sound_effects_softness": {
          "name": "Sound effects softness",
          "description": "Softness of the sound-reactive overlay (0–100)."
        }
      }
    },
    "set_music_mode": {
      "name": "Set music mode",
      "description": "Configure the H6199 music mode with adjustable parameters.",
      "fields": {
        "mode": {
          "name": "Mode",
          "description": "Music sub-mode: energic, rhythm, spectrum, or rolling."
        },
        "sensitivity": {
          "name": "Sensitivity",
          "description": "Microphone sensitivity (0–100)."
        },
        "color": {
          "name": "Color",
          "description": "Optional accent color as [R, G, B]."
        }
      }
    }
  }
}
